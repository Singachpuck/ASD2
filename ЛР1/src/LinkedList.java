public class LinkedList {
    private static class Node{
        Object element;
        Node next;
        Node prev;

        public Node(Object element)
        {
            this.element = element;
        }
    }
}
